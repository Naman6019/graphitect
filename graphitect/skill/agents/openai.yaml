interface:
  display_name: "Graphitect"
  short_description: "Generate a grounded interactive codebase architecture report"
  default_prompt: "Use Graphitect's host-model workflow to create an evidence-grounded, self-contained architecture report for this repository without a separate API key."
